<%= form_for(email_user) do |f| %>
  <% if email_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(email_user.errors.count, "error") %> prohibited this email_user from being saved:</h2>

      <ul>
      <% email_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :login %>
    <%= f.text_field :login %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
